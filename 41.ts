/* Make an array of magician’s names. Pass the array to a function called show_magicians(), which 
prints the name of each magician in the array. */


let megician_Names = ["Ali", "Ahmed", "Faraz", "naeem"];

function show_magicians(){
    for(let Megicians of megician_Names){
        console.log(Megicians)
}
}
show_magicians();    // call of function