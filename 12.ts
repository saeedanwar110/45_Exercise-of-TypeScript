/*
Start with the array you uesed in Exercise 11, but instead of just printing each person`s name, print a message to them. The text of each message should be the same, but each message should be personalized with the person`s name.  
*/

let friendNames: string[] = ["Karam", "Mohsin", "Ali Ahmed", "Mudsar"];
             // index nymber    0         1          2           3

             console.log(`Hello ${friendNames[0]}, how are you?`);
             console.log(`Hello ${friendNames[1]}, how are you?`);
             console.log(`Hello ${friendNames[3]}, how are you?`);
             console.log(`Hello ${friendNames[2]}, how are you?`);